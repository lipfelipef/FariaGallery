---
import { render } from 'astro:content';
import Base from '../../layouts/Base.astro';
import Topo from '../../components/Topo.astro';
import Rodape from '../../components/Rodape.astro';
import { postsPublicados, dataPorExtenso, dataISO, type Post } from '../../lib/blog';

export async function getStaticPaths() {
  const posts = await postsPublicados();
  return posts.map((post) => ({ params: { slug: post.id }, props: { post } }));
}

interface Props {
  post: Post;
}

const { post } = Astro.props;
const { Content } = await render(post);
---

<Base titulo={post.data.titulo} descricao={post.data.resumo}>
  <Topo />

  <main id="conteudo">
    <article class="wall">
      <header class="cabeca hang">
        <div class="cabeca-corpo">
          <h1 class="work-title titulo">{post.data.titulo}</h1>
          <p class="resumo">{post.data.resumo}</p>
        </div>

        <dl class="ficha">
          <dt class="field">Publicado</dt>
          <dd class="value">
            <time datetime={dataISO(post.data.data)}>
              {dataPorExtenso(post.data.data)}
            </time>
          </dd>

          {
            post.data.atualizado && (
              <>
                <dt class="field">Atualizado</dt>
                <dd class="value">
                  <time datetime={dataISO(post.data.atualizado)}>
                    {dataPorExtenso(post.data.atualizado)}
                  </time>
                </dd>
              </>
            )
          }

          {
            post.data.assuntos.length > 0 && (
              <>
                <dt class="field">Assunto</dt>
                <dd class="value">{post.data.assuntos.join(', ')}</dd>
              </>
            )
          }
        </dl>
      </header>

      <div class="hang">
        <div class="prosa">
          <Content />
        </div>
      </div>

      <p class="volta hang">
        <span></span>
        <a class="quiet-link" href="/blog">Voltar para o blog</a>
      </p>
    </article>
  </main>

  <Rodape />
</Base>

<style>
  .cabeca {
    padding-block: clamp(3rem, 10vh, 5.5rem) clamp(2rem, 5vh, 3rem);
    row-gap: 0.5rem;
    border-bottom: 1px solid var(--color-rule);
  }

  .cabeca-corpo {
    grid-column: 2 / -1;
    margin-bottom: 1.5rem;
  }

  @media (max-width: 47.99rem) {
    .cabeca-corpo {
      grid-column: 1 / -1;
    }
  }

  .titulo {
    font-size: clamp(2.125rem, 1.3rem + 3.4vw, 3.25rem);
  }

  .resumo {
    margin-top: 1rem;
    max-width: 46ch;
    font-size: 1.0625rem;
    line-height: 1.6;
    color: var(--color-ink-soft);
  }

  .ficha {
    grid-column: 1 / -1;
    display: grid;
    grid-template-columns: var(--hang) minmax(0, 1fr);
    column-gap: var(--gutter);
    row-gap: 0.5rem;
  }

  @media (max-width: 47.99rem) {
    .ficha {
      grid-template-columns: minmax(0, 1fr);
      row-gap: 0.125rem;
    }
  }

  .prosa {
    grid-column: 2 / -1;
    padding-block: clamp(2.5rem, 6vh, 4rem);
  }

  @media (max-width: 47.99rem) {
    .prosa {
      grid-column: 1 / -1;
    }
  }

  .volta {
    padding-bottom: clamp(2rem, 6vh, 4rem);
    font-size: 0.875rem;
  }
</style>
